import Foundation

// MARK: - Stub Implementation for Initial Build

/// Defect overlay (stubbed)
struct DefectOverlayNode: Sendable {
    let id: UUID

    init(id: UUID = UUID()) {
        self.id = id
    }
}
