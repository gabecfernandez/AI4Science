import Foundation

// MARK: - Stub Implementation for Initial Build

/// Bounding box AR overlay (stubbed - not using SCNNode)
struct BoundingBoxOverlay: Sendable {
    let id: UUID

    init(id: UUID = UUID()) {
        self.id = id
    }
}
