import Foundation

// MARK: - Stub Implementation for Initial Build

/// Label overlay (stubbed)
struct LabelOverlayNode: Sendable {
    let id: UUID

    init(id: UUID = UUID()) {
        self.id = id
    }
}
