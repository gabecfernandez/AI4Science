import Foundation

// MARK: - Stub Implementation for Initial Build

/// Measurement overlay (stubbed)
struct MeasurementOverlayNode: Sendable {
    let id: UUID

    init(id: UUID = UUID()) {
        self.id = id
    }
}
